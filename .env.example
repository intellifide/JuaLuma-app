# Core local database
POSTGRES_USER=jualuma_user
POSTGRES_PASSWORD=change-me
POSTGRES_DB=jualuma_db
DATABASE_URL=postgresql://jualuma_user:change-me@localhost:5433/jualuma_db

# Backend required app config
APP_ENV=local
FRONTEND_URL=http://localhost:5175
BACKEND_CORS_ORIGINS=http://localhost:5175,http://localhost:5176,http://localhost:5177
GCP_PROJECT_ID=jualuma-dev
GOOGLE_CLOUD_PROJECT=jualuma-dev
GMAIL_IMPERSONATE_USER=hello@jualuma.com
MAIL_CONTACT_HELLO=hello@jualuma.com
SUPPORT_EMAIL=support@jualuma.com
LOCAL_ENCRYPTION_KEY=local-dev-key-change-me
JOB_RUNNER_SECRET=local-job-runner-secret

# Plaid (required by backend settings)
PLAID_CLIENT_ID=plaid-client-id
PLAID_SECRET=plaid-secret
PLAID_ENV=production
# Optional for production OAuth + webhook delivery
# PLAID_REDIRECT_URI=https://app.example.com/connect-accounts
# PLAID_WEBHOOK_URL=https://api.example.com/webhook/plaid
# PLAID_ANDROID_PACKAGE_NAME=com.example.app

# Stripe (set test keys for local)
STRIPE_PUBLISHABLE_KEY=pk_test_replace_me
STRIPE_SECRET_KEY=sk_test_replace_me
STRIPE_WEBHOOK_SECRET=whsec_replace_me

# Tatum hard-cutover provider contract (active Web3 provider)
TATUM_API_KEY=replace_me
TATUM_BASE_URL=https://api.tatum.io
TATUM_TIMEOUT_SECONDS=15
TATUM_RETRY_MAX_ATTEMPTS=3
TATUM_RETRY_BASE_BACKOFF_MS=250

# Frontend/local build-time vars
VITE_GCP_API_KEY=replace_me
VITE_API_BASE_URL=http://localhost:8001
# Dev-only login bypass for localhost UI iteration (never enable outside local)
VITE_LOCAL_AUTH_BYPASS=false
NEXT_PUBLIC_APP_URL=http://localhost:5175
NEXT_PUBLIC_API_URL=http://localhost:8001

# Optional AI defaults (local)
AI_MODEL=gemini-2.5-flash
AI_MODEL_PROD=gemini-2.5-flash

# Optional if using Gmail API credentials in local workflows
# GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/sa.json
